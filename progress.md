## In Progress
- Waiting for Windows build/CI verification (new project + app update cases)

## Completed
- Backend: stop silent baseline installs during auto-link
- Backend: baseline signature + info/install commands
- Backend: persist baseline ack in state.json
- Frontend: one-time baseline prompt + install action

## Blocked
